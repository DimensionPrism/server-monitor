server_id = "server-a"
host = "127.0.0.1"
port = 9000
repo_paths = ["/work/project-a", "/work/project-b"]
metrics_interval_seconds = 3
status_interval_seconds = 12

# Optional command overrides (defaults are Linux-oriented and usually enough)
# system_cmd = ["bash", "-lc", "echo 'CPU: 1'; echo 'MEM: 2'; echo 'DISK: 3'; echo 'RX_KBPS: 0'; echo 'TX_KBPS: 0'"]
# gpu_cmd = ["bash", "-lc", "nvidia-smi --query-gpu=index,name,utilization.gpu,memory.used,memory.total,temperature.gpu --format=csv,noheader,nounits"]
# git_status_cmd = ["git", "-C", "{repo}", "status", "--porcelain", "--branch"]
# clash_status_cmd = ["bash", "-lc", "if pgrep -f clash >/dev/null; then echo running=true; else echo running=false; fi; echo api_reachable=false; echo ui_reachable=false; echo message=ok"]
